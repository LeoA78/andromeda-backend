# Se agregan las properties propias del proyecto
#
# MS Settings.
#jdbc:mysql://localhost:3306/practica_jpa_comercio?serverTimezone=UTC
spring:
  # Datasource settings (se debe agregar la información de la base de datos local)
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${DB_HOST}:${DB_PORT}/${DB_DATABASE}?serverTimezone=UTC
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    hibernate:
      ddl-auto: update
    defer-datasource-initialization: 'true'
  sql:
    init:
      mode: always
      data-locations: classpath:data.sql
  mail:
    properties:
      mail:
        smtp:
          starttls:
            enable: 'true'
            required: 'true'
          auth: 'true'
          quitwait: 'false'
    host: smtp.mailgun.org
    username: postmaster@sandbox0de8e9c61e504df2aa667c12ddfe85d3.mailgun.org
    protocol: smtp
    password: 39727c038e33be1737f0bbe3e8309261-b0ed5083-8545c768
    port: '587'

# Swagger settings.
springfox:
  documentation:
    swagger:
      v2:
        path: /swagger.json